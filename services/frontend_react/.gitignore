# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Local .env files (use .env.example for public template)
.env
.env.local
.env.*.local

# Cloud-specific build env files — commit intentional ones only via explicit git add
# (staging non-secrets are currently tracked; secrets must never appear here)

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
