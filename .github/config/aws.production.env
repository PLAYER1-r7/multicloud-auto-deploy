# AWS production — committed static config (single source of truth)
# Dynamic values (Cognito IDs, bucket names, distribution IDs) come from Pulumi outputs.
# This file is intentionally NOT gitignored so CI can read it reliably.
# Update this file when infrastructure values change (e.g., ACM cert renewal).

CLOUD_CUSTOM_DOMAIN="www.aws.ashnova.jp"
CLOUD_ACM_CERT_ARN="arn:aws:acm:us-east-1:278280499340:certificate/fafdb594-5de6-4072-9576-e4af6b6e3487"
# CloudFront origin hostname (d????.cloudfront.net) — used as fallback if Pulumi output empty
CLOUD_CLOUDFRONT_DOMAIN="d1qob7569mn5nw.cloudfront.net"
CLOUD_CLOUDFRONT_DISTRIBUTION_ID="E214XONKTXJEJD"

# Security settings — Task S1 (2026-03-04)
# Temporarily disabled due to IAM permission requirements
# CLOUD_CLOUDTRAIL_ENABLED="true"
# Redeployment trigger - pydantic-core 2.23.2
