# AWS Lambda Requirements (Using Klayers for main dependencies)
# Only include dependencies NOT available in Klayers

# Core dependencies (provided by Klayers public layers)
# fastapi==0.115.0  # Klayers-p312-fastapi
# pydantic==2.9.0  # Included in fastapi layer
# uvicorn[standard]==0.32.0  # Not needed in Lambda
# python-jose[cryptography]==3.3.0  # Klayers-p312-python-jose
# requests==2.32.3  # Klayers-p312-requests
# mangum==0.17.0  # Klayers-p312-mangum

# Required dependencies not in Klayers
pydantic-settings==2.5.2
python-multipart==0.0.9
pyjwt==2.9.0

# AWS-specific dependencies
boto3==1.35.0

# Database
# psycopg2-binary==2.9.9  # Only if using RDS PostgreSQL
# sqlalchemy==2.0.35  # Only if using ORM
